//obtiene el momento actual
var ahora = new Date();
//Obtiene el momento de después de tiempo

class Tree{
	constructor(){
		//Genera el arbol como tal
		this.root = null;

	}

	ad (node) {
		//funciones para agregar nodos...

		}

// aqui se ejecuta el contructor de Tree
var Tree = new Tree();
Tree.add("prueba");


function Tree() {
	this.root = null;
}

add(val){
	if (this.isEmpty()){
		this.root=new Node(val)
		return
	}
}
var personas = this.root
while (aux) {
	// a la izquierda
	if (val < aux.value){
		if (aux=aux.left)
	} else {
		aux.left=new Node (val)
		return
	}
} else {
	// vamos hacia la derecha
	if (aux.right) {
		aux = aux.right
	} else {
		aux.right=new Node (val)
		return
	}
   
  
